{
  "name": "cvpalmanord-static-clone",
  "version": "1.0.0",
  "private": true,
  "description": "Clon estatico de cvpalmanord.es listo para Railway",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "start:legacy": "sh -c 'serve site -l ${PORT:-3000}'",
    "audit:deps": "npm audit --omit=dev"
  },
  "dependencies": {
    "serve-handler": "6.1.7"
  }
}

